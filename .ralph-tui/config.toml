configVersion = "2.1"
maxIterations = 10
agent = "claude"
tracker = "beads-rust"
autoCommit = true
subagentTracingDetail = "full"

[agentOptions]
[trackerOptions]
[errorHandling]
strategy = "retry"
maxRetries = 2
retryDelayMs = 5000
